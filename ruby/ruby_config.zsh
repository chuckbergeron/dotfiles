# using ruby-build to install ruby's

source /usr/local/share/chruby/chruby.sh
source /usr/local/share/chruby/auto.sh

chruby ruby-2.3.1

# if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
